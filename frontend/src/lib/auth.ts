// Autenticação não configurada ainda.
// Para habilitar NextAuth, instale as dependências e configure:
//   npm install next-auth bcryptjs @types/bcryptjs
// Depois crie src/app/api/auth/[...nextauth]/route.ts com o handler.

export {};
